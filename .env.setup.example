# === GCP ===
export GCP_PROJECT_ID="your-gcp-project-id"
export GCP_PROJECT_NUMBER=000000000000         # GCP コンソールで確認
export GCP_SA_NAME="money-diary-sa"
export GCP_SA_EMAIL="${GCP_SA_NAME}@${GCP_PROJECT_ID}.iam.gserviceaccount.com"
export GCP_WIF_POOL_ID="money-diary-pool"
export GCP_WIF_PROVIDER_ID="money-diary-aws"
export GOOGLE_CLIENT_ID=000000000000-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.apps.googleusercontent.com  # OAuth 同意画面で取得

# === AWS ===
export AWS_ACCOUNT_ID="$(aws sts get-caller-identity --query Account --output text)"
export AWS_REGION="ap-northeast-1"
export S3_BUCKET="money-diary-frontend-${AWS_ACCOUNT_ID}"
export SAM_STACK_NAME="money-diary"
export LAMBDA_FUNCTION_NAME=""        # sam deploy 後に取得
export CF_DISTRIBUTION_ID=""          # CloudFront 作成後に取得
export CF_DOMAIN=""                   # CloudFront 作成後に取得

# === Spreadsheet (移行時のみ) ===
export SPREADSHEET_ID=""
